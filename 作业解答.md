# 第 10 周作业 · 解答

> 关联文档：[作业要求](作业要求.md)
> 关联代码：`~/development/AIGC/learning/AI_Video/week-10/gradio-mini-agent/`
> GitHub 提交仓库：（待填）

---

## Task 1 · Skill 系统与多模态联动分析

### Skill 加载与执行机制

下面三段是我读完代码后按"先猜测、再验证、再修正"的顺序整理的笔记。

#### A1 · 发现机制：我以为有缓存，结果没有

起初我猜 Skill 应该是**启动时扫一次缓存住**——这是大部分系统的常见做法。但 `grep -rn "get_skills_context" backend/app/` 后只看到两处命中：定义在 [skill_service.py:170](backend/app/services/skill_service.py#L170)，调用方在 [agent_service.py:73](backend/app/services/agent_service.py#L73)。再往上追，调用 `get_skills_context()` 的 `create_agent()` 又被 [agent_service.py:111](backend/app/services/agent_service.py#L111) 在每次 `process_chat_stream` 里**重新触发**。

读到 [scan_available_skills](backend/app/services/skill_service.py#L68) 才真正意识到：**它没有 `@lru_cache`、没有全局变量、什么缓存都没有**——每次都现读磁盘，遍历 `skills/public/` 和 `skills/custom/`，用正则 + `yaml.safe_load` 解析 frontmatter，提取 name / description / id / source / md_path 五个字段。这"看似低效"的设计正是为了让用户在设置页改了 Skill 启用开关后**不需要重启服务**——这就是 README 里"Skill 修改后无需重启"的实现机制。

#### A2 · Progressive Loading：原来"渐进"是这样省 token 的

我对 "Progressive Loading" 这个词最初的理解很模糊，以为是某种"分块加载"。读完 [get_skills_context](backend/app/services/skill_service.py#L170) 才搞清楚：

它注入到 system prompt 里的 XML 块**只含元数据**——每个 skill 一个 `<skill>` 节点，仅 name + description + location（SKILL.md 路径），**正文一个字节都不进 prompt**。

那 Agent 怎么获取完整工作流？答案在 [skill_tools.py:54](backend/app/tools/skill_tools.py#L54) 的 `read_skill_file_tool`——一个标准的 LangChain `@tool`，签名是 `(path, start_line?, end_line?) -> str`。Agent 在 ReAct 循环里**自己**判断"我该读这个 SKILL.md 了"，然后主动 emit 一个 tool call。

意外发现这个工具甚至支持按行范围读取——SKILL.md 很长时还能分段加载。所以 system prompt 的 token 占用是常数级，**与 skill 数量、SKILL.md 长度都解耦**。这才是 "Progressive" 的精髓。

#### A3 · 工具选择本质：一次认知翻转

我凭直觉觉得"靠 LLM 自觉照做"简直天方夜谭——如果 SKILL.md 写"先调 A 工具再调 B 工具"，怎么可能没有调度器强制执行？所以我猜**有硬编码执行器**。

直到读到 [agent_service.py:82-87](backend/app/services/agent_service.py#L82)：

```python
agent = create_react_agent(name=..., model=..., tools=tools, prompt=full_prompt)
```

只传了 4 个参数。**没有 `steps`、没有 `workflow`、没有 `pipeline`、没有任何描述"按顺序做事"的结构化字段**。SKILL.md 里的"步骤 1→2→3"只能通过 `prompt=full_prompt` 这条路径进来——它就是 prompt 字符串里的一段普通文字。**我猜错了——是纯 prompt 工程**。

这次翻转让我意识到：Skill 系统的可靠性 = LLM 指令遵循能力 × prompt 措辞质量。本质上和 system prompt 第一行写"你是友好助手"是**同一个机制**——都是 instruction following。这也解释了为什么实际开发中会发现 Skill 偶尔不被正确激活——这是 LLM 概率性输出的天然特性，不是"bug"。

#### 调用链与机制图

下图把上面三段的发现整理成一张图：上半部分（蓝色）是"注入期"的同步调用链，下半部分（绿色）是"运行期"Agent 自主按需加载的过程，中间虚线就是 Progressive Loading 的核心边界。

![Skill 加载机制与 Progressive Loading](assets/skill-mechanism.png)

### 多模态联动工作流

下面三段以「智能播客生成」为例。所有结论都来自我两次真实实验的后端日志（`backend/logs/polystudio_20260427.log`），不是凭空推理——失败和成功都有迹可循。

#### B1 · Agent 第一步做什么——一次失败 + 一次成功才看清这个工具

我打开 [tools/qwen_omni_understanding.py](backend/app/tools/qwen_omni_understanding.py) 找入口，[第 223 行](backend/app/tools/qwen_omni_understanding.py#L223) 就是被 `@tool` 装饰的函数：

```python
@tool("qwen_omni_understand", args_schema=QwenOmniUnderstandInput)
def qwen_omni_understand_tool(
    media_path: str,                       # 必填：媒体文件路径
    question: str,                         # 必填：自然语言提问
    media_type: Optional[str] = None,      # 可选：image/audio/video，留空按扩展名自动识别
    output_audio: bool = True,             # 可选：是否同时返回语音回答
    voice: str = "Cherry",                 # 可选：语音音色
) -> str:
```

最初我把 5 个参数都当必填——后来才意识到 Python 语法里**有 `=` 默认值的就是可选**，真正必填只有 `media_path` 和 `question`。这工具支持图片 / 音频 / 视频三类（[docstring 第 234-237 行](backend/app/tools/qwen_omni_understanding.py#L234)）。

返回类型注解写的是 `-> str`，但读到 [第 319-335 行](backend/app/tools/qwen_omni_understanding.py#L319) 才发现里面装的是 `json.dumps(result, ...)`——一个 **JSON 序列化后的字符串**，dict 结构含 `text_response`（主要分析文字）、`media_type`、`audio_url`（可选）等。

我的实验给这两件事都做了实证：

- **失败案例（21:13:21+）**：上传 32.7 MB 视频，三次连续 400 错误：`String value length (28046504) exceeds the maximum allowed (28000000)`。视频 base64 编码后超了百炼 API 的字符串上限 28 MB——**只多了不到 50 KB**。
- **成功案例（21:29:53）**：换成 10.6 MB 视频立刻通过，返回 `text_len=745`，并附带一段 `omni_20260427_212953.wav`（因为 `output_audio` 默认 True）。

这个 28 MB 上限是 Qwen3-Omni 当前的硬约束，以后写 SKILL.md 要在前置检查里挡住超大文件。

#### B2 · 理解结果如何传给下一个工具——message-passing 的"啊哈"时刻

我的直觉是"qwen_omni 的输出会被某段代码塞进下一个工具的入参"——肯定有某个 wiring 逻辑在串。

读 [stream_processor.py](backend/app/services/stream_processor.py) 看到这一行才反应过来：

```python
agent.astream(
    {"messages": langchain_messages},        # ← Agent 的"内存"就是这个 list
    {"recursion_limit": self.recursion_limit},
    stream_mode=["messages"],
)
```

**根本没有"传"，是 LangGraph 的 messages 列表在生长**：

1. `qwen_omni_understand` 返回那串 JSON 字符串
2. LangGraph 把字符串包成一个 `ToolMessage`，**追加到 messages 列表末尾**
3. 下一轮 LLM 被调用时，看到的 input 是**完整的 messages 列表**——包括刚刚那个 ToolMessage
4. LLM **自己**读 JSON 串里的 `text_response`，**自己**决定下一步调哪个工具、**自己**填参数

所以"工具间传递数据"是个错觉。**所有数据都汇集到 messages 这块共享黑板上，每轮 LLM 都看完整黑板，自主决定下一笔。**

我的 Test 4 日志给了这个判断毫不含糊的实证——同一次任务里 `messages_count` 从 10 → 16 → 20 → 24 → 65 → 67 → 69 单调递增，每多一次工具调用就 +2（一个 AIMessage 带 tool_calls + 一个 ToolMessage 带结果）。这个列表就是 Agent 的"记忆"。

旁边的 `recursion_limit=200`（[backend/.env](backend/.env)）则是这块黑板增长的硬上限，防止 LLM 反复调工具陷入死循环。

#### B3 · 从视频到成品播客的完整工具调用链

我的 Test 4（21:45-21:57）实际跑通了一遍——日志里每个时刻都对应一次工具调用，串起来就是这条链：

```
21:45:46  read_skill_file × 2           [Progressive Loading 加载 podcast-creator/SKILL.md]
              ↓
21:48:02  qwen_voice_design             [设计主持人音色样本]
21:50:25  qwen_voice_design             [设计嘉宾音色样本]
              ↓
21:51:58  qwen_voice_cloning            [段 1：开场白]
21:53:04  qwen_voice_cloning            [段 2：曹操诸葛亮场景]
21:53:32+ qwen_voice_cloning            [段 3..N，逐段克隆]
              ↓
21:56:30  concatenate_audio             [把 N 段 wav 拼成完整对话]
              ↓
21:56:39  select_background_music       [按主题智能选 BGM]
21:56:51  mix_audio_with_bgm            [BGM 开场 3 秒后渐弱到 5%]
              ↓
          🎉 成品播客 .mp3
```

读完这条链我有 3 个实打实的发现（每个都同时对应 Task 1.A 里的某条结论）：

1. **第一步是 `read_skill_file` 而不是 `qwen_omni_understand`**——印证了 A1/A2 的 Progressive Loading：Agent 先靠元数据判断匹配，再主动 read 全文拿到工作流，最后才执行业务工具。
2. **`voice_cloning` 不是单次而是多次**——SKILL.md 没有硬编码"做几段"，是 LLM 看完脚本后自己拆段、自己决定调几次。又一次实证了 A3 的"纯 prompt 工程"。
3. **整个流程花了约 12 分钟**——其中 LLM 思考、用户多轮确认占了大头。如果做成无人值守 Skill，理论上能压到 3-5 分钟。**这就是 Task 2 要解决的核心问题。**

### 多模态二创流水线图

把 B1-B3 整合成一张图：每个箭头上方是工具名 + Test 4 实际时刻，下方是数据形态；底部黑板示意 messages 列表的状态共享。

![智能播客二创流水线](assets/podcast-pipeline.png)

---

## Task 2 · 自动化多模态二创 Skill 设计

### 🎯 30 秒看懂

| 难点 | 我的应对 | 一句话原理 |
|---|---|---|
| 现有 podcast-creator 做播客要用户配合 5-6 步确认，太累 | 写一份手册让 AI 自己一气呵成 | "店员按手册办事" |
| AI 看了手册总想抄近路（直接用原音频不重做） | 撞墙 4 次，每次把手册改得更狠 | "出错就改手册" |
| 改对了吗？ | 第 5 次实测：6 段全是 AI 新生成，原音频没拼进去 | "实测铁证" |

**最终交付物**：[backend/skills/custom/media2podcast/SKILL.md](backend/skills/custom/media2podcast/SKILL.md)（245 行）+ 2 个 references 文件——一份给 AI 看的"工作手册"。

---

### 我做了什么

题目要求"用户上传视频/音频 → AI 自动做成播客，中间不打扰用户"。我选最硬核的"直接更新项目"——产出可以直接跑的 SKILL.md。

不需要改任何 Python 代码，**纯 prompt 工程**——这正是 Task 1.A3 翻转后的认知（Agent 听不听话全靠 prompt 措辞）。文件写完，下次对话 AI 自动扫到。

**与现有 `podcast-creator` 的边界划分**：

- `podcast-creator`：用户给主题文字 → 创作播客（**交互式**，每步等用户确认）
- `media2podcast`（我新增）：用户给上传文件 → 二创播客（**无人值守**，一次跑完）

两者通过 description 关键词区分（"上传的视频或音频" vs. "纯主题文字"）让 AI 自主分流。

---

### 借用 vs 原创——把账算清楚

我没有从零发明工作流——大方承认借了 podcast-creator 哪些东西，自己原创了哪些：

| 维度 | media2podcast | 来源 |
|---|---|---|
| **下游 4 个工具调用顺序**（voice_design / voice_cloning / concatenate_audio / mix_audio_with_bgm） | 同 podcast-creator | 🟡 **借用**（作业要求"调用相应的生成工具"，本就该复用） |
| **`references/` 子目录结构** | 同 podcast-creator / video-creator | 🟡 **借用**（第 4 次撞墙后才学到的主流做法） |
| **Step 1 前置检查（门禁）** | 没有对应 | 🟢 **原创**——podcast-creator 不接受文件上传 |
| **Step 2 `qwen_omni_understand` 调用** | 没有对应 | 🟢 **原创**——podcast-creator 从文字主题开始，根本不用 omni |
| **Step 3 "基于 omni 输出改写"的逻辑** | 逻辑相反 | 🟢 **原创**——podcast-creator 从主题创作，本 Skill 从已有内容提炼 |
| **"无人值守"约束**（铁律 5 条 + 严禁询问） | 设计相反 | 🟢 **原创**——podcast-creator 是**交互式**，每步都让用户确认 |
| **生命周期定义**（SOURCE_MEDIA → SEGMENTS） | 没有对应 | 🟢 **原创**——podcast-creator 不存在"上传文件消耗"问题 |
| **Step 6 audio_files 白名单审核** | 没有对应 | 🟢 **原创**——第 2 次撞墙后实测推出的修复 |
| **18 MB 门禁阈值** | 没有对应 | 🟢 **原创**——基于实测 API 双重限制推出 |
| **anti_patterns.md 4 个失败案例** | 没有对应 | 🟢 **原创**——我自己实测踩出来的坑 |

**核心创新点（多模态理解触发 + 二创语义 + 无人值守约束 + 4 个失败案例）都是 podcast-creator 没有的**。

---

### 为什么拆成 7 步？——把上次跑通的链路写进手册

Task 1.B 段我们已经实测过 podcast-creator 在文字主题下能跑通的工具链：

```
read_skill → omni_understand → (LLM 写脚本) → voice_design → voice_cloning ×N → concatenate_audio → mix_audio_with_bgm
```

我没另发明流程，**把这条链路一字不漏地写进 media2podcast 的"手册"**，让 AI 自动按 7 步跑。每一步都规定 4 件事：**目标 / 用什么工具 / 没说时用什么默认值 / 怎么算这步成功**。

5 个最关键的设计决策：

| 决策 | 我选了 | 为什么 |
|---|---|---|
| 单人独白 vs 双人对谈 | 单人 | MVP 省 50% API |
| 播客时长 | 3 分钟 ≈ 500 字 | 看得见结果但不烧钱 |
| 音色样本怎么造 | 用固定试听文本造样本，N 段全部重做克隆 | 一致性 > 省 1 次 API |
| 失败重试次数 | 1 次 | 70% 抖动能挡住，3 次性价比骤降 |
| BGM 库为空 | 跳过混音直接交付 | 优雅降级，不影响主流程 |

---

### 写完手册后我撞了 3 次墙

我不止"写完就交差"——真机测了，发现 AI 各种"创造性"地偷懒。每次撞墙都把手册改得更严。

#### 第 1 次撞墙：脚本写完，AI 就把脚本当成品发给我了

**那次跑成什么样**（日志 2026-04-28 22:22-22:23）：

```
22:22:14  AI 读了 media2podcast 手册              ✅
22:22:36  AI 调用 qwen_omni_understand             ✅
22:23:24  Omni 返回视频内容摘要                     ✅
22:23:27  AI 开始写脚本                             ✅
22:23:51  ⚠️ 流式完成——AI 把脚本当最终答复给我了
```

AI 把脚本当成最终交付，**没继续走第 4-7 步**。

**问题出在哪**：手册没明说"必须 7 步全完成"。AI 看到自己输出了一长段文字，本能地觉得"任务完成"。

**我怎么修的**：在手册顶部加了"铁律 5 条"——第 1 条直接说"任何中间产出都不是最终答案"。Step 3 末尾还加了一句"对 AI 喊话"："如果你刚写完脚本，停止把它告诉用户的念头，立即调 qwen_voice_design_tool"——这种反直觉的措辞业内叫 anthropomorphic instruction，但意外地有效。

#### 第 2 次撞墙：AI 把我上传的原音频"包装"成播客

**那次跑成什么样**（日志 2026-04-28 22:38-22:41）：

AI 调了 omni 理解、调了 voice_design 造开场、又调了 1 次 voice_cloning 造结尾，然后调拼接：

```
audio_files = [
  voice_design_xxx_欢迎收听....wav,    ← AI 开场
  upload_xxxx_660576fe.mp3,            ← ❌ 我上传的原音频！
  voice_cloning_xxx_感谢收听....wav    ← AI 结尾
]
```

**最终成品 = AI 开场 + 我上传的原音频整段 + AI 结尾**——完全偏离了"二创"的本意。

**问题出在哪**：手册里没明确说"上传的文件用一次就消耗了"。AI 看到 messages 历史里那个 upload_xxx.mp3 文件路径，本能地觉得"这是合法资源"——就像实习生看到客户给的食材，懒得加工就直接端上桌。

**我怎么修的**：给数据流里每个"东西"显式定义生命周期：

| 名称 | 来源 | 什么时候用 | 进不进成品 |
|---|---|---|---|
| `SOURCE_MEDIA` | 用户上传的文件 | **Step 2 用一次就消耗** | ❌ |
| `VOICE_SAMPLE` | voice_design 输出 | 仅 Step 5 当参考 | ❌ |
| `SEGMENTS[N]` | voice_cloning ×N 输出 | Step 6 拼接源 | ✅ **唯一成品来源** |

然后在 Step 6 加了"白名单审核"：

| 路径前缀 | 处理 |
|---|---|
| `voice_cloning_*.wav` | ✅ 必须全部纳入 |
| `upload_*.mp3` | ❌ 严禁——SOURCE_MEDIA 已消耗 |
| `voice_design_*.wav` | ❌ 严禁——音色样本不进成品 |

#### 第 3 次撞墙：门禁阈值定的比 API 真实上限还宽

**那次跑成什么样**（日志 2026-05-02 12:16-12:36）：

我上传了一个 20.7 MB 音频。手册里 Step 1 写的"≤ 25 MB 才放行"——文件**通过门禁**。然后调 omni：

```
12:16:18  AI 调 omni（文件 base64 编码后传输）
12:26:19  ⏳ openai 客户端第 1 次重试
12:36:20  ⏳ 第 2 次重试
12:36:26  ❌ 报错：Exceeded limit on max bytes per data-uri item: 20971520
```

**整整 20 分钟看不到任何反馈**——前端早 timeout 显示"断了"。

**问题出在哪**：百炼 API 实际有**两条**互相独立的硬限制：

| 限制 | 等价原文件大小 |
|---|---|
| String value length ≤ 28 MB（base64 字符串长度） | 约 21 MB |
| Max bytes per data-uri item ≤ 20 MB（**原文件二进制**） | **20 MB**（更严） |

之前只测过超 28MB string 的场景，没遇过单纯超 20MB binary 的——所以阈值定 25 MB 时没暴露问题。

**我怎么修的**：阈值 **25 MB → 18 MB**（比 API 真实上限留 2 MB 缓冲）。

---

### 第 4 个更狡猾的发现：连 AI 听不听 Skill 的话本身都不稳定

修完前 3 个问题，本以为稳了。换了文件再测，AI 又把原音频拼进成品，**和第 2 次撞墙一模一样**。

但这次原因不同——日志显示这次 AI **根本没读 SKILL.md**：

```
13:01:27  收到聊天请求
13:01:41  AI 直接调 qwen_omni_understand    ← 跳过了 read_skill_file！
（无 🎯 命中 skill 日志）
```

**问题出在哪**：Skill 是启用的，元数据也注入了 system prompt，**但 AI 没"识别到"匹配，就走了裸路径**——所有手册约束都失效。

我用同样的文件**两次跑结果完全不同**：

| 时刻 | 是否触发 Skill | 结果 |
|---|---|---|
| 11:35（用户措辞含"做播客二创"） | ✅ 命中 | 6 段 voice_cloning 跑通 |
| 13:01（用户措辞简单） | ❌ 没命中 | 走裸路径，又把原音频拼进去 |

**这是更深层的发现**：在生产环境，"无人值守"必须假设 **Skill 命中本身可能失败**——SKILL.md 写的所有约束都建立在"被加载"的前提上。

**怎么应对**：用户请求里**显式带触发词**（"请用 media2podcast Skill..."）——AI 看到 skill 字面名时几乎不可能漏匹配。

---

### 回头看才意识到：我在闭门造车

第 4 次撞墙后我去读了项目自带的 podcast-creator 和 video-creator 怎么写——**发现自己之前完全没参考主流做法**。

| 我之前的做法 | 主流做法（podcast-creator/video-creator） |
|---|---|
| 把所有禁令、默认值、案例堆在主 SKILL.md（320 行） | 主文件简短，把示例和案例放 `references/` 子目录 |
| 用抽象列表（"❌ 严禁 A、严禁 B、严禁 C"） | 用"场景挂规则"（"有角色 → 用 edit；空镜 → 用 generate"） |
| 关键约束塞在汇总章节最后 | 关键约束**就近**重复在工具调用语境 |

**我做了一次重构**——把主 SKILL.md 瘦到 245 行，拆出两个 references 文件：

```
backend/skills/custom/media2podcast/
├── SKILL.md                       ← 245 行（瘦身后）
└── references/
    ├── workflow_example.md         ← 完整成功调用链
    └── anti_patterns.md            ← 4 个失败案例 + 修复约束
```

`anti_patterns.md` 把上面 4 次撞墙的故事都收进去——下次 AI 要做拼接前，可以**主动读这个文件**复核（这就是 Task 1.A2 学过的 Progressive Loading 在自家 Skill 里的应用）。

---

### 重构后第 5 次实测——日志铁证

2026-05-02 13:10-13:17，**带触发词**重测，全程 7 分钟：

| 时刻 | 工具 | 关键参数 | 是否合规 |
|---|---|---|---|
| 13:10:59 | `read_skill_file` | media2podcast/SKILL.md | ✅ Skill 命中 |
| 13:11:06 | (AI 文字) | "前置检查通过：路径=...mp3..." | ✅ Step 1 显式 self-check |
| 13:11:23 | `qwen_omni_understand` | question = "请详细描述这段媒体..." | ✅ **完全按手册固定那句** |
| 13:12:58 | `qwen_voice_design` | text = "大家好，欢迎收听..." | ✅ **试听文本=固定那句**（前几次都没遵守！）|
| 13:13:26 - 13:16:51 | `qwen_voice_cloning` × **6 次** | 6 段对话脚本 | ✅ **N=6（5-7 区间内）** |
| 13:17:15 | `concatenate_audio` | audio_files = **6 个 voice_cloning_\*.wav** | ✅✅✅ **没有 upload_\***！|
| 13:17:22 | `select_background_music` | scene="神秘预言..." → Kitchen_Table_Light.mp3 | ✅ |
| 13:17:33 | `mix_audio_with_bgm` | bgm_volume=-26dB, intro=3s | ✅ **Step 7 完整闭环** |
| 13:17:50 | (完成) | | ✅ |

**最关键的一行**——concatenate_audio 的 audio_files：

```
[
  voice_cloning_xxx_段1.wav,    voice_cloning_xxx_段2.wav,
  voice_cloning_xxx_段3.wav,    voice_cloning_xxx_段4.wav,
  voice_cloning_xxx_段5.wav,    voice_cloning_xxx_段6.wav
]
```

**6 个文件全部 voice_cloning_ 前缀，没有任何 upload_，原音频没被拼进去**——这是从 v1 → v3 三轮迭代的最终验证点。

---

### v1 → v2 → v3 三版对比

| 维度 | v1（最初 320 行） | v2（加铁律补丁） | v3（参考主流重构） |
|---|---|---|---|
| 行数 | 320 | 320+ | 245 + 2 references |
| 中途停下 | ❌ 会停 | ✅ 修复 | ✅ |
| 把原音频拼进去 | ❌ 会做 | ❌ 还会做 | ✅ 修复 |
| 阈值正确 | ❌ 25MB 太宽 | ❌ | ✅ 改 18MB |
| 试听文本固定 | ❌ AI 自由发挥 | ⚠️ 偶尔遵守 | ✅ |
| voice_cloning 段数 | ❌ 只 1 段 | ⚠️ | ✅ 6 段 |
| Step 7 BGM 闭环 | — | — | ✅ 真机跑通 |
| 设计风格 | 闭门造车 | 打补丁 | 借鉴主流 podcast-creator |
| 真机验证 | — | — | ✅ 2026-05-02 13:17 |

---

### 5 条可迁移到下个 Skill 的设计沉淀

1. **手册不是文档，是 prompt**——每个字都是给 AI 的指令，不是给人看的解释
2. **"踩坑→改手册"是常态**——不要指望一次写对，AI 总能找到你没堵的捷径
3. **关键约束要"就近重复"**——埋在底部汇总章节里，AI 走到工具调用时早忘了
4. **给数据流里每个"东西"取个名 + 写生命周期**——让 AI 知道"这玩意儿什么时候应该消失"
5. **借鉴 sibling Skill 比闭门造车强**——项目自带 podcast-creator/video-creator 是免费教科书

---

### 集成改动点

| 改动 | 路径 | 影响 |
|---|---|---|
| 新增 `SKILL.md` 主文件 | `backend/skills/custom/media2podcast/SKILL.md`（245 行） | 自动被 [skill_service.py:68](backend/app/services/skill_service.py#L68) 扫到，无需重启 |
| 新增 `references/workflow_example.md` | 完整成功调用链 | AI 可主动 `read_skill_file` 加载 |
| 新增 `references/anti_patterns.md` | 4 个失败案例 + 修复约束 | AI 在 Step 5/6 调用前主动复核 |
| Python 代码 | **无改动** | 100% prompt 工程 |
| 新增工具 | **无新增** | 复用现有 19 个工具 |

---

## 自查清单

- [ ] Task 1 分析约 200 字
- [ ] Task 1 配流程图
- [ ] Task 2 任选一种或组合交付
- [ ] 推送到 GitHub / Gitee 并填入仓库链接
- [ ] 在作业评论框提交链接
