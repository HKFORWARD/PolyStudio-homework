# 第 4 周作业 · 长视频 Agent 多角色与动态时长扩展

> 极客时间 · 多模态 Agent 开发实战营 · 第一次课后作业【必做】

---

## 配套资料

| 资料 | 说明 | 链接 |
|------|------|------|
| 作业原文 | 极客时间作业完整页面（飞书） | [打开](https://my.feishu.cn/docx/HgShdXEPxocP16xiz1vcvHolnAd) |
| 第四章实验文档 | 视频模型生成实验配套讲义（飞书） | [打开](https://my.feishu.cn/docx/V2VJdY5tFo8nyIxLxqJccNJunXd) |
| Agent 仓库分支说明 | 各 Agent 实践代码分支导航（飞书） | [打开](https://my.feishu.cn/docx/QBXBdfAVYoYKmuxLuRMcu3WTnyf) |
| PolyStudio 项目 | 作业目标项目源码（GitHub） | [打开](https://github.com/hirogoing/PolyStudio) |

> [!tip] 链接使用顺序建议
> 1. 先读「第四章实验文档」补全理论与流程理解
> 2. 用「分支说明」找到本周对应分支，clone「PolyStudio」到本地
> 3. 对照下方代码位置阅读实现，再动手做作业

---

## 一、题目背景

> 现有设计主要体现在 **单角色一致性** 和 **固定时长**。现在从 **多角色** 和 **动态时长** 两个维度思考扩展方案 —— 包括生图模型是否调整（多角色）、流程节点（如时长分配与校验）以及是否引入新工具或新参数。

### PolyStudio 长视频流程速览

```
用户输入 → 分镜生成（等用户确认）→ 按分镜为每个镜头生图 → 图生视频 → 拼接成片
```

### 涉及代码位置

| 模块 | 文件 / 工具 |
|------|------------|
| Agent 与工作流描述 | `backend/app/services/agent_service.py`（长视频工作流 Prompt 约 92–149 行） |
| 生图工具 | `backend/app/tools/volcano_image_generation.py` —— `generate_volcano_image_tool`、`edit_volcano_image_tool` |
| 图生视频 | `backend/app/tools/volcano_video_generation.py` —— `generate_volcano_video_tool` |
| 视频拼接 | `backend/app/tools/video_concatenation.py` —— `concatenate_videos_tool` |

---

## 二、作业要求

### Task 1 · 分析当前长视频 Agent 的基础能力

> 结合代码（上述文件及 Prompt 段落）完成简要分析，约 200 字，可配简图（流程图 / 数据流图）。

需要回答三个问题：

- [ ] **现有工作流**：从用户输入到成片，Agent 依次做了哪些步骤？涉及哪些工具？数据如何在步骤间传递（如 分镜 → 镜头描述 → 生图 → 图生视频 → 拼接）？
- [ ] **现有角色一致性**：当前如何保证同一角色在多镜头中一致？依赖什么输入（如一张角色图）、什么工具（`edit` vs `generate`）、Prompt 中的哪些规则？
- [ ] **现有时长处理**：分镜中如何体现时长（如「镜头1：5 秒」）？单段视频生成的时长由谁决定（用户 / 分镜 / 默认）？拼接后是否对总时长校验或调整？接口返回时长与请求不一致时如何处理？

---

### Task 2 · 增加多角色一致性能力

> 当前仅支持「一个角色 + 多镜头」：所有出现该角色的镜头都基于同一张角色图做 `edit`。

**目标**：支持同一部长视频中出现多个不同角色（如角色 A、角色 B），每个角色在多镜头中形象一致，且角色之间互不混淆。

**任选一种或组合交付**：

- [ ] **方案与架构**：文字 + 基础架构图说明扩展方式
    - 如何表示「角色 ↔ 参考图」对应关系（角色 ID + 图片路径）
    - 分镜 / 镜头描述中如何标识本镜头有哪些角色
    - Agent 在生图时如何为每个镜头选择正确的参考图并调用 `edit`
- [ ] **伪代码**：对关键流程给出伪代码或步骤列表
    - 根据分镜与角色表，为每个镜头分配合适的参考图
    - 决定用 `generate` 还是 `edit`
- [ ] **直接更新项目**：在现有代码上实现多角色一致性
    - 扩展 Prompt
    - 在对话 / 上下文中维护「角色名 / ID → 参考图路径」的映射
    - 生图阶段根据镜头描述选择参考图

---

> [!note] 关于"动态时长"
> 题目背景中"多角色和动态时长两个维度"是引导你**思考**的方向提示，不是独立 Task。
> 动态时长部分已通过 **Task 1 第 3 问"现有时长处理"** 体现（分镜时长写法、单段决定方、总时长校验、接口不一致处理），无需单独提扩展方案。

---

## 提交说明（极客时间）

- 作业写在自己的 GitHub（word、pdf、代码均可）
- 把 GitHub 链接放入「作业提交入口」交给助教批改 —— **不要直接复制内容到提交框**
- 必须按文档要求提交完整作业才会批改
- 关注「极客时间训练营」公众号查看批改完成提示
